<footer>
  <div class="container">
    <p class="text-muted">
      <% if current_user && current_user.admin == 1 %>
        Signed in as <%= current_user.full_name %> (<%= link_to 'Log out', session_path('current'), method: :delete %>) | <%= link_to 'Admin', admin_users_path %>
      <% elsif current_user %>
        Signed in as <%= current_user.full_name %> (<%= link_to 'Log out', session_path('current'), method: :delete %>)
      <% else %>
        <%= link_to 'Log in', new_session_path %> | <%= link_to 'Sign Up', new_user_path %>
      <% end %>
    </p>
  </div>
</footer>
