<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/movies" id="home"><img class="navbar-image" src="/logo-alt.png"/></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="dropdown nav-li">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hello, <%= current_user.firstname.capitalize %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><a href="/users/<%= current_user.id %>">Profile</a></li>
              <li><a href="#">Account</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#" data-toggle="modal" data-target="#newMovie" id="addMovieNav">Add a Movie</a></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to 'Log out', session_path('current'), method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-li"><a href="#" data-toggle="modal" data-target="#signUp">Sign Up</a></li>
          <li class="nav-li"><a href="#" data-toggle="modal" data-target="#login">Login</a></li>
        <% end %>
      </ul>    
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<div class="col-md-2">
  <%= render '/movies/new_movie_modal' %>
  <%= render '/sessions/sign_up_modal' %>
  <%= render '/sessions/login_modal' %>
</div>