<div class="col-md-12" id="results">
  <!-- Index/Search/Show results -->

  <% if @movies %>
    <% @movies.each do |movie| %>
      <%= render partial: 'movie', locals: { movie: movie } %>
    <% end %>
  <% end %>

  <%= paginate @movies %>
  <%= page_entries_info @movies %>

</div>
